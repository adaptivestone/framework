doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
  body
    h1 #{t('email.passwordRecovery', { defaultValue: 'Recovery password' })}
    p #{t('email.passwordChanged', { defaultValue: 'Password changed' })} #{link}

